# redis-clone
Redis (Remote Dictionary Server) is an open-source, in-memory data structure store used as a database, cache, and message broker. It stores data in computer RAM rather than on traditional hard disks, allowing it to process read and write operations in less than a millisecond
